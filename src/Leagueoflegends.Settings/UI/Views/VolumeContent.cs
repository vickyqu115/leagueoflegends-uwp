using Jamesnet.Core;
using Windows.UI.Xaml.Controls;

namespace Leagueoflegends.Settings.UI.Views;

public class VolumeContent : ContentControl, IView
{
    public VolumeContent()
    { 
        DefaultStyleKey = typeof(VolumeContent);
    }
}
