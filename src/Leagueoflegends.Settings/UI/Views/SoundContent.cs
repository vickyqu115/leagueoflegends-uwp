using Jamesnet.Core;
using Windows.UI.Xaml.Controls;

namespace Leagueoflegends.Settings.UI.Views;

public class SoundContent : ContentControl, IView
{
    public SoundContent()
    { 
        DefaultStyleKey = typeof(SoundContent);
    }
}
