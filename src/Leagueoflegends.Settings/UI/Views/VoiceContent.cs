using Jamesnet.Core;
using Windows.UI.Xaml.Controls;

namespace Leagueoflegends.Settings.UI.Views;

public class VoiceContent : ContentControl, IView
{
    public VoiceContent()
    { 
        DefaultStyleKey = typeof(VoiceContent);
    }
}
